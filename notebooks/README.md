# Notebooks

Exploration only. **No result in the paper may originate here** — every reported
number comes from `results/runs` via `scripts/make_figures.py` and
`scripts/make_tables.py`.

Suggested scratch order:
1. `01_channel_sanity` — CIR shape against the closed form
2. `02_testbed_calibration` — fitting the twin to real traces
3. `03_attack_exploration` — payload compression under the symbol budget
4. `04_monitor_thresholds` — divergence distributions on benign runs
