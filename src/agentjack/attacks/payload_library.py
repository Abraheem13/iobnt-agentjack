"""
Curated + compressed injection payloads with provenance tags.
"""

# TODO(contract):
#   - load_payloads(source) -> list[Payload]
#   - compress_to_budget(text, n_bits) -> symbols   # the molecular bottleneck
#   - Every payload records its source corpus for the paper's data statement.
