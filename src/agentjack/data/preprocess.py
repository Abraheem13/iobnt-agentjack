"""
Shared preprocessing: resampling, windowing, normalisation, splits.
"""

# TODO(contract):
#   - resample_trace(df, dt)
#   - window(series, length, stride)
#   - patient_level_split(...)  # never split within a patient
