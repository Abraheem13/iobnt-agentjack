"""
Tier-D loader: curated SBML models for biological actuation response.
"""

# TODO(contract):
#   - load_sbml(model_id) -> simulator handle (tellurium/libroadrunner)
#   - Optional: keep behind a feature flag to protect the timeline.
