"""
Tier-C loaders: prompt-injection payload corpora (attack material only).
"""

# TODO(contract):
#   - load_agentdojo(path) / load_injecagent(path) / load_bipia(path) / load_asb(path)
#   - Canonical output: list[Payload(text, goal_type, source, license)]
#   - We use payload TEXT + taxonomy, not their environments.
