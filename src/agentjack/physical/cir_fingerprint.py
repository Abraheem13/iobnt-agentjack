"""
Channel-impulse-response fingerprinting = the FAST timescale of the defense.
"""

# TODO(contract):
#   - estimate_cir(observation, known_preamble) -> np.ndarray
#   - fingerprint_distance(cir_a, cir_b) -> float
#   - class CIRAuthenticator: fit(legit_traces); score(observation) -> float
#   - This is also the standalone PLA baseline (defenses/pla_cir.py wraps it).
