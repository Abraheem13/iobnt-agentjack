"""
Cross-timescale divergence criterion used to fuse monitor levels.
"""

# TODO(contract):
#   - level_divergence(level_stats, reference) -> float
#   - fuse(divergences, weights) -> trust_score
#   - calibrate_thresholds(benign_runs, target_fpr) -> thresholds
